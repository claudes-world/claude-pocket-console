# Short code block

Here is a small TypeScript snippet:

```ts
function greet(name: string): string {
  return `Hello, ${name}!`;
}

greet("world");
```

And a short bash example:

```bash
pnpm install
pnpm test
```
